% --- INSERTED (Results) ---
\paragraph{Summary (added).}
Under mesh refinement and decreasing time step, the method exhibits the expected decrease of the error in standard norms (see \cref{fig:individual_convergence}). 
The spatial error follows the approximation order of the chosen finite-dimensional space, whereas the temporal error shows approximately [first/second]-order behavior up to the point where the total error is dominated by the spatial component. 
Interquartile bands across regions of interest indicate stable convergence with no pronounced local outliers. 
For reproducibility, we report $\Delta t \in [\ldots]$, representative element sizes $h \in [\ldots]$, total degrees of freedom (DOFs) $[\ldots]$, and the norms used for evaluation (e.g., $L^2$, $H^1$, $L^\infty$).

\medskip
\noindent\textbf{Physical invariants.}
At fixed mesh resolution and varying time step (\cref{fig:physical-checks}), integral invariants (e.g., mass/energy/momentum) remain within the bounds of the numerical error over a broad range of $\Delta t$.
We report the invariant error as [absolute/relative] with respect to a [closed-form / over-resolved numerical] reference after convergence of the inner sub-iterations at each step.
Only for the largest time steps do we observe a systematic drift attributable to time-discretization error; in practice, the recommended regime lies where the invariants remain flat within the reported spread.

\medskip
\noindent\textbf{Nonlinear solver performance.}
The average number of fixed-point subiterations per time step grows mildly with DOFs (see \cref{fig:performance}), while wall-clock time is dominated by linear solves and communication (see \cref{fig:performance}). 
Smaller $\Delta t$ typically improves time-to-solution (faster contraction), whereas robust convergence at larger $\Delta t$ is enabled by acceleration.
% --- /INSERTED (Results) ---

% --- ORIGINAL CONTENT BELOW (preserved) ---
\subsection*{Convergence and performance}

We verify spatial consistency using field–norm error estimates on nested meshes with non-matching transfer. All quantities are dimensional; averages are normalized by $|\Omega|$.

\paragraph{Meshes and notation.}
Let $\{\mathcal{T}_i\}_{i=1}^L$ be a family of tetrahedral meshes on $\Omega$ obtained by uniform refinement, with characteristic sizes $h_i=1/N_i$ (we report $h_i=1/N_i$ explicitly). On each $\mathcal{T}_i$ we solve the fully coupled problem to a fixed time $T$ with \emph{the same} $\Delta t$ and model parameters; this avoids additional time–discretization bias from remounting left–hand sides. Denote by
\[
\mathbf{u}_i,\quad S_i,\quad \rho_i,\quad \mathbf{A}_i
\]
the discrete fields on level $i$ in their conforming $H^1$ spaces $V_{h_i},Q_{h_i},Q_{h_i},T_{h_i}$. Because the solution can exhibit sharp internal layers (bone/void fronts), we do not assume high regularity.

\paragraph{Field–norm pair errors on the finer mesh.}
For two consecutive levels $(H,h)=(i,i{+}1)$ we evaluate errors on the finer mesh in a \emph{higher–order observation space}. For any field $X\in\{\mathbf{u},S,\rho,\mathbf{A}\}$ we construct $W_{h}^{(X)}$ by raising the polynomial degree by $\Delta p=2$, transfer $X_H$ to $W_{h}^{(X)}$ by non-matching interpolation, and interpolate the fine-field values into the same space. With $\mathcal{I}_{H\to h}$ denoting the non-matching interpolation and $\widetilde X_h$ the interpolated fine solution, the pair error reads
\[
e_{H\to h}^{X,\;L^2} \;=\; \big\| \widetilde X_h - \mathcal{I}_{H\to h} X_H \big\|_{L^2(\Omega)},
\qquad
e_{H\to h}^{X,\;H^1\text{-semi}} \;=\; \big\| \nabla(\widetilde X_h - \mathcal{I}_{H\to h} X_H) \big\|_{L^2(\Omega)} .
\]
Vector/tensor seminorms use the canonical componentwise sum. All integrals are evaluated with sixth-order quadrature on $\mathcal{T}_h$. For a sequence of pairs we obtain $\{(h_{i+1},e_{i\to i+1}^{X,\bullet})\}_{i=1}^{L-1}$ and estimate an experimental order of convergence (EOC) $p$ from the log–log fit
\[
\log e \approx p\,\log h + \mathrm{const},\qquad
p \;=\; \arg\min_{p} \sum_{i} \big(\log e_{i\to i+1}^{X,\bullet} - p\log h_{i+1} - c\big)^2 ,
\]
reported for both $L^2$ and $H^1$–semi norms. When only three levels are available, we also quote the two–ratio estimate $p \approx \log(e_{H\to h}/e_{h\to h/2})/\log(r)$ with $r=h_H/h_h$.


\begin{figure}[htbp]
  \centering
  \IfFileExists{images/convergence_plot.png}{%
    \includegraphics[width=0.92\linewidth]{images/convergence_plot.png}%
  }{\fbox{\parbox{0.9\linewidth}{\centering Missing image: \texttt{images/convergence_plot.png}}}}
  \caption{Convergence of spatial and temporal errors under mesh and time-step refinement. Solid lines: errors (norms specified in the text). Dotted lines: ideal slopes for $P^1$ elements and the Backward--Euler scheme. Shaded bands show interquartile ranges across regions of interest.}
  \label{fig:individual_convergence}
\end{figure}

\paragraph{Spatial convergence}
At fixed $\Delta t$, errors decrease monotonically under mesh refinement and pairwise orders stabilize toward the theoretical rates. Local deviations near high gradients reflect limited smoothness but do not break the global trend (\cref{fig:individual_convergence}).

\paragraph{Temporal convergence}
Decreasing $\Delta t$ shows an initial sublinear regime (transient adaptation) followed by the asymptotic behavior indicated by the dotted reference slope.

\paragraph{Physical checks}
We verify basic conservation and invariants at a fixed mesh resolution while varying the timestep (\cref{fig:physical-checks}). For each timestep we plot, over physical time, (a) internal versus external work in mechanics and (b) the corresponding relative energy-balance residual; (c) stimulus power-balance residual (absolute/relative); (d) density mass-balance residual (absolute/relative); and for the direction field, (e) the spatially averaged traces $\mathrm{tr}(\mathbf{A})$ and $\mathrm{tr}(\widehat{\mathbf{M}})$ and (f) the trace residual. Curves use converged sub-iterations per step. As $\Delta t$ decreases, balance residuals decrease and invariants remain bounded as expected.

\begin{figure}[htbp]
  \centering
  \IfFileExists{images/physical_checks_plot.png}{%
    \includegraphics[width=1\linewidth]{images/physical_checks_plot.png}%
  }{\fbox{\parbox{0.9\linewidth}{\centering Missing image: \texttt{images/physical_checks_plot.png}}}}
  \caption{Physical conservation and invariants at fixed mesh resolution and varying timestep. Panels (a)--(f) show, respectively: mechanical internal/external work, mechanical energy residual (relative), stimulus power-balance residual (absolute/relative), density mass-balance residual (absolute/relative), traces $\mathrm{tr}(\mathbf{A})$ and $\mathrm{tr}(\widehat{\mathbf{M}})$, and the trace residual. One line per $\Delta t$; quantities are evaluated on converged sub-iterations per step.}
  \label{fig:physical-checks}
\end{figure}

\paragraph{Solver performance}
We profile per-timestep cost and memory across meshes and time steps (\cref{fig:performance}). Metrics are aggregated across the simulated horizon: means for subiterations and memory; sums for linear iterations and walltime. The figure summarizes how iteration counts, walltime, memory, and the distribution across subsolvers vary with total DOFs and timestep size.

\begin{figure}[htbp]
  \centering
  \IfFileExists{images/performance_plot.png}{%
    \includegraphics[width=1\linewidth]{images/performance_plot.png}%
  }{\fbox{\parbox{0.9\linewidth}{\centering Missing image: \texttt{images/performance_plot.png}}}}
  \caption{Solver performance summary. Panel (a): fixed-point subiterations per timestep versus total DOFs for several $\Delta t$. Panels (b)--(d) at fixed $\Delta t=25$ days: (b) cumulative KSP iterations per timestep by subsolver, (c) memory consumption (total across ranks and maximum per rank), and (d) cumulative walltime per timestep by subsolver, all versus total DOFs. Axes are log--log.}
  \label{fig:performance}
\end{figure}
